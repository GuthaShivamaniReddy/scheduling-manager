{
  "$schema": "https://turbo.build/schema.json",
  "globalEnv": ["DATABASE_URL", "WEBHOOK_RECEIVER_URL", "WEBHOOK_SECRET"],
  "ui": "tui",
  "tasks": {
    "build": {
      "dependsOn": ["^build"],
      "inputs": ["$TURBO_DEFAULT$", ".env*"],
      "outputs": ["dist/**"],
      "env": ["DATABASE_URL", "WEBHOOK_RECEIVER_URL", "WEBHOOK_SECRET"]
    },
    "start": {},
    "type-check": {},
    "lint": {},
    "dev": {
      "cache": false,
      "persistent": true,
      "env": ["DATABASE_URL", "WEBHOOK_RECEIVER_URL", "WEBHOOK_SECRET"]
    },
    "test": {},
    "generate": {},
    "drop": {},
    "migrate": {
      "cache": false
    },
    "seed": {
      "cache": false
    },
    "studio": {}
  }
}
